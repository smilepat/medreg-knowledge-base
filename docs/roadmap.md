# 실행 로드맵 (단계별 체크리스트)

> 원칙: **한 파일/한 스크립트씩**, 검증 후 다음. 문서 1건으로 끝까지 통과 → 규모 확대.

---

## 🎯 Milestone 1 — PoC: 문서 1건 전체 통과 (현재)

목표: **규정 1개 문서**를 추출→구조화→검색→인용까지 한 번에 통과시켜 파이프라인 검증.

### M1-0. 범위 확정
- [x] 대상 규정 1건 선정 → **의료기기법(본법)** (2026-06-15 확정)
  - 근거: 위계 가장 정제(파싱 검증 최적) + 제2조 정의·교차참조 풍부(정의어 사전·링크 검증) + 표 적음(난이도↓) + 모든 하위규정의 뿌리 노드
  - 다음 순서: #2 허가·신고·심사 규정(고시 2026-6호, 표/별표 도전) → #3 GMP 고시(입찰)
- [x] 원문 형식 확인 → **law.go.kr 정제 텍스트** (HWP/스캔 함정 없음)
- [ ] `raw/`에 의료기기법 원문 배치 (law.go.kr에서 확보)

### M1-1. 추출 (extract.py)
- [ ] `scripts/extract.py` 작성 — 샘플 1건 텍스트+표 추출
- [x] **법령은 Open API XML 경로 채택** → `fetch_law.py` (조/항/호 위계가 구조화돼 옴, PDF 불필요)
- [x] `extract.py` 표 보존 검증 (스모크 테스트 통과) — 외부 PDF/입찰공고용으로 보존
- [ ] 추출 도구 비교 메모 (Upstage / LlamaParse / Docling) → `docs/extraction-tools.md` *(M2, 외부 PDF 등장 시)*

### M1-2. 구조화 (structure.py) ✅
- [x] 장(章) 헤더 vs 조문 구분, 장 맥락 부착
- [x] 법령 위계(조/항/호) 파싱 → 계층 트리 + 가지조(제N조의M)
- [x] 출력: Markdown(계층 헤더) + JSON 이중 구조 → `processed/의료기기법_structured.{json,md}`
- [x] 검증: 조문 91 / 항 302 / 호 282 / 장 8

### M1-3. 청킹 + 메타데이터 (chunk.py) ✅
- [x] 조항 단위 청킹 (contextual: `[법령명 제N조(제목)] 장` 맥락 줄 포함)
- [x] 메타데이터 필수 6필드 + is_current/cross_refs/defined_terms 태깅
- [x] 정의어 사전 분리(41개) / 교차참조 추출(청크 69개)
- [x] **산출물: `samples/의료기기법_processed.json` (91 청크)** ← M1 핵심 검증물 ✅
- [ ] (정밀화 보류) 「외부법」 바로 뒤 제N조를 내부참조로 오인하는 케이스 보정

### M1-4. 저장 + 검색 (embed.py / search.py)
- [ ] Supabase pgvector 스키마 생성 (별 테이블)
- [ ] 청크 임베딩 + 적재
- [ ] 하이브리드 검색(의미+키워드) + 메타데이터 필터
- [ ] 테스트 질문 3~5개로 검색 품질 확인

### M1-5. 활용 PoC (보고서 인용)
- [ ] 질문→검색→**출처 인용** 초안 생성 1건
- [ ] 환각 방지 확인 (근거 없으면 "확인 불가" 나오는가)

---

## 📈 Milestone 2 — 규모 확대 (수백 페이지)
- [ ] 대상 규정 목록 전체 확정 + 우선순위
- [ ] 추출 도구 최종 채택
- [ ] 배치 파이프라인화 (전 규정 일괄 처리)
- [ ] 버전 관리(시행일) 체계 구현

## 🏗️ Milestone 3 — 입찰 컴플라이언스 매칭
- [ ] 공고문 요구사항 추출 → requirement matrix
- [ ] 규정+회사자료 대조 → 충족/미충족/확인필요 판정
- [ ] HITL 검토 UI

---

## 다음 작업 (Next Action)
> `scripts/extract.py` 하나만 작성 + 샘플 PDF 1개로 표 보존 테스트.
